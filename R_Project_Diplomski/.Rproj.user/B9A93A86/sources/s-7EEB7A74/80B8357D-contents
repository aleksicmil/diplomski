## Ucitam radni dataset

radni <- read.table("Data/radni_dataset.txt", sep = "\t", header = T)

## Treba mi lissta svih proteina u radnom da bi ih prevela u ENTREZ

radni_prot <- as.data.frame(radni$Protein)
write.table("Data/radni_proteini.txt", sep = "\t", row.names = F, 
            col.names = F, quote = F)

## Mapping table sa UniProta, prevedeno UniProtKB AC/ID u RefSeq Protein ID

mapa <- read.table("Data/radni_UniProt_u_RefSeq.txt", header = T, sep = "\t")
