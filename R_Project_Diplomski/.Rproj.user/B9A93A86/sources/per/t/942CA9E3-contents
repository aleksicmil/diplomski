## Ucitam fajl sa resenjima

pann <- read.table("Data/pannzer_rezultati.txt", sep = "\t", header = T)

pan_cc <- subset(pann, pann$type == "CC_ARGOT")
pan_cc$type <- as.factor(as.character(pan_cc$type))
